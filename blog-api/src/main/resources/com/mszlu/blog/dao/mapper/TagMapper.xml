<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mszlu.blog.dao.mapper.TagMapper">
    <select id="selectTagByArticleId" parameterType="long" resultType="com.mszlu.blog.dao.pojo.Tag">
        select id, avatar, tag_name tagName
        from ms_tag
        where id in
              (select tag_id from ms_article_tag where article_id = #{articleId})
    </select>

    <!--List<Long> selectTagIdByLimit(int limit);-->
    <select id="selectTagIdByLimit" parameterType="list" resultType="java.lang.Long">
        select tag_id
        from ms_article_tag
        GROUP BY tag_id
        ORDER BY COUNT(article_id)
        DESC limit #{limit}
    </select>

    <!--List<Tag> selectTagsByTagId(List<Long> tagIdList);-->
    <select id="selectTagsByTagId" parameterType="list" resultType="com.mszlu.blog.dao.pojo.Tag">
        select id,avatar, tag_name tagName
        from ms_tag
        where id in
        <foreach collection="tagIdList" item="tagId" open="(" close=")" separator=",">
            #{tagId}
        </foreach>
    </select>
</mapper>
